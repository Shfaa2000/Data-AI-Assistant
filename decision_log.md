# Decision Log — Cloud Billing Dataset Cleaning

### القرار 1 — التعامل مع AvailabilityZone عند Microsoft
- السياق: كل صفوف Microsoft (51/51) فيها AvailabilityZone = ContractedUnitPrice = ListUnitPrice
  بالضبط، دليل قاطع على انزياح أعمدة بمصدر البيانات، مش قيمة منطقة حقيقية.
- الخيارات: (1) حذف العمود كاملاً، (2) تركه بدون إجراء، (3) الإبقاء عليه + عمود Boolean
  يعلّم موثوقيته لكل صف.
- المعايير: عدم فقدان معلومة (AWS وOracle فيهم قيم AZ حقيقية)، وقابلية التتبع لاحقاً.
- الدليل: تطابق 100% بين الأعمدة الثلاثة، فقط عند Microsoft.
- القرار: الخيار 3 — عمود AvailabilityZone_valid (Boolean) يعلّم صفوف Microsoft بـFalse.
- النتائج: أي تحليل جغرافي مستقبلي لازم يفلتر AvailabilityZone_valid == True أولاً.
- متى نراجعه: لو تأكدنا من مصدر القيمة الحقيقية، أو انضافت بيانات Microsoft بصيغة مختلفة.

### القرار 2 — تفسير القيم السالبة عند Microsoft (Usage rows)
- السياق: 12 صف سالب صغير جداً (من 1e-9 إلى 0.16)، داخل ChargeCategory=Usage، بدون
  كلمة Credit/Refund/Promotional بالوصف.
- البحث: توثيق Microsoft الرسمي يؤكد نوعين شائعين: (1) rounding adjustments صغيرة جداً
  بعد الفاتورة، (2) adjustment charges/back-dated credits مدمجة بنفس سطر الاستخدام.
- القرار: نفسّرها كتصحيحات فوترة طبيعية (rounding/adjustment)، مش أخطاء — لا تُحذف.
- الحد: ما نقدر نجزم نوع كل صف بدون سجل الفوترة الأصلي عند Microsoft — استنتاج مبني
  على نمط عام موثق، مش دليل مباشر لكل صف.

### القرار 3 — عدم دمج مفاتيح Tags المتشابهة دلالياً (env مقابل environment)
- السياق: بعد تطبيع case/whitespace، بقي "env" (42 مرة) و"environment" (660 مرة)
  كمفتاحين منفصلين — مختلفين بالحروف نفسها، مش بسبب حالة الأحرف.
- الخيارات: (1) دمجهم كمفتاح واحد بافتراض إنهم بنفس المعنى، (2) الإبقاء عليهم منفصلين
  لحد ما يثبت التطابق بدليل أقوى.
- المعايير: تجنّب افتراض غير مثبت قد يُفقد تمييزاً حقيقياً بين الحقلين.
- القرار: الخيار 2 — نبقيهم منفصلين حالياً.
- النتائج: أي تحليل على Tags لازم يعامل env وenvironment كفئتين منفصلتين لحد إشعار آخر.
- متى نراجعه: لو ظهر دليل إضافي (بحث بمعيار FOCUS، أو نمط استخدام واضح) يثبت التطابق.

### القرار 4 — تصنيف الأعمدة عالية الفقدان
- السياق: أربعة أعمدة بنسب فقدان عالية جداً: ChargeClass (100%), CommitmentDiscountName
  (100%), ResourceName (94.9%), ResourceType (79.1%).
- المعايير: الفرق بين "Not Applicable" (فاضي بالكامل، الحقل غير منطبق) و"Missing"
  (فاضي جزئياً، يعتمد على مصدر البيانات).
- القرار: ChargeClass وCommitmentDiscountName = Not Applicable (فاضيين 100% بلا استثناء).
  ResourceName وResourceType = Missing متوقع (فقدان متفاوت حسب provider، مش عمود ميت).
- النتائج: لا حذف لأي عمود من الأربعة؛ التوثيق فقط يوجّه أي تحليل مستقبلي يعتمد عليهم.


### القرار 5 — استبدال IQR بـAdjusted Boxplot لاكتشاف outliers
- السياق: IQR أعطت 222 outlier (22% من البيانات) — غير منطقي لبيانات skewness=3.17.
- الخيارات: (1) البقاء على IQR رغم عيوبها، (2) رفع k لـ3، (3) Adjusted Boxplot (medcouple).
- الدليل: k=3 خفضت العدد لـ184 فقط (تحسن ضعيف). Adjusted Boxplot خفضته لـ87.
- القرار: الخيار 3 — عمود BilledCost_outlier بطريقة medcouple.
- الحد: medcouple=0.998 يتجاوز مدى الصحة الموصى به (-0.6/0.6) — النتيجة تقريبية،
  تُستخدم كإشارة للمراجعة اليدوية مش كحقيقة مطلقة.
- متى نراجعه: لو تغيّرت طبيعة العينة (أضيفت بيانات provider جديد بتوزيع مختلف).






